# הוראות התקנה לבעלים שכבר יש לו MAKERS, לקלוד קוד

הקובץ הזה מיועד אליך, הסוכן שמתקין. הבעלים לא צריך לקרוא אותו.

⛔ **וזה לא המסמך שמסביר את התשתית.** ההסבר המלא, מה צריך להתקיים ולמה כל חלק שם, יושב אצל הבעלים ב-`2-makers/vibecoder/refs/transcript-infrastructure.md`, והוא מגיע לשם בשלב 3. **קרא אותו לפני הקובץ הזה.** הקובץ הזה הוא הצעדים; הוא הסיבות, והוא מה שמאפשר לך לבנות את זה גם כשמשהו במכונה הזאת לא מתאים למה שכתוב כאן.

## עצור, ובחר את הקובץ הנכון

יש כאן שני קבצי התקנה, והם לא מתחלפים.

`INSTALL.md` הוא לבעלים שאין לו כלום. הוא **מקים** לו בית זיכרון מאפס, כמה קבצי טקסט בתיקייה אחת, ומחבר אליהם את הצינור.

**הקובץ הזה הוא למי שכבר יש לו מערכת MAKERS.** יש לו צוות מייקרים, יש לו `4-learned/`, יש לו מורטי, והוא פותח את אותה תיקייה בכל יום. אצלו אין מה להקים, יש מה לחבר.

**אם תריץ כאן את `INSTALL.md`, תקים לו בית זיכרון שני ליד זה שכבר יש לו, ותזרים את הפגישות שלו לתוך תיקייה שאף סוכן לא קורא.** זו התקלה היקרה ביותר בקובץ הזה, והיא נראית מוצלחת לגמרי מבחוץ.

## עצור אם אתה לא קלוד קוד

הצינור מריץ את הבינארי של קלוד קוד (`claude -p`) פעמיים ביום, ומתחבר ל-Wispr Flow ול-Google Drive דרך מחברי ה-MCP של claude.ai. סוכן אחר יכול לקרוא את הקבצים כאן, אבל לא יכול להשלים את ההתקנה ולא יכול להריץ את הצינור. אל תתקין חצי מערכת: אמור את זה במשפט אחד ועצור.

## מה שונה אצלו, וזה כל ההבדל

ארבעה דברים, וכולם נובעים מכך שהמערכת שלו כבר בנויה.

**טבלת הניתוב מגיעה מלאה.** אצל מי שאין לו מערכת, קלוד ממלא את הטור הימני ביד תוך כדי התקנה, ומנחש לאן ילך כל סוג של עובדה. כאן אנחנו יודעים בדיוק: כתיבה לכותב, מיצוב למשווק, עיצוב למעצב, משהו שרץ לוייבקודר. הטבלה יושבת ממולאת ב-`2-makers/scribe/scribe.md` שלב 7. אל תכתוב אותה מחדש ואל תשנה אותה.

**המתעד מצטרף כאיש צוות, לא כתהליך רקע.** הוא מקבל תיקייה משלו, שורה בטבלת הצוות של מורטי ודלת פקודה. בלי השורה בטבלה, אף עבודה לא מנותבת אליו ואף שגיאה לא נזרקת: הוא פשוט לא קיים בשביל המערכת.

**ההפעלה היא משפט שהוא אומר למורטי.** הוא פותח את תיקיית MAKERS שהוא פותח בכל יום, ואומר שהוא רוצה שהפגישות שלו יגיעו לצוות. זה פותר לבד את "פתחתי בתיקייה הלא נכונה", שהוא הכשל השכיח ביותר בגרסה הציבורית.

**ערוץ הפלט השני חי מהיום הראשון.** זוויות מיצוב והתנגדויות משיחות לידים נוחתות ב-`5-library/messaging/` ו-`5-library/objections/`, והמשווק כבר מופנה לשניהם מקובץ המלאכה שלו.

## ⛔ הדבר שישבור לו את המערכת אם תפספס אותו

בקובץ `.claude/rules/records.md` שלו כתוב מפורשות שרק הארכיונאי כותב ל-`4-learned/`, וב-`4-learned/state.md` יש תקרה של **25 כללים**.

`state.md` נטען לתוך כל סשן. ריצה שרצה פעמיים ביום ומוסיפה לשם בולטים מפוצצת את התקרה תוך שבועיים, ואז הקובץ היחיד שמנהיג את כל הצוות מפסיק להיות משהו שהבעלים יכול להחזיק בראש.

**אצלו הריצה כותבת ל-`4-learned/log.md` ולקבצי המלאכה של המייקרים, ומציעה את הכלל הקבוע במקום לכתוב אותו.** ההצעה נוחתת בדוח היומי ובשורה חדשה ב-`3-work/now/transcript-absorption/_process/proposed-rules.md`, והארכיונאי מכריע בדיבריף הבא.

שתי הגנות, ושתיהן חייבות להיות במקום:

1. `state.md` **לא נמצא** ברשימת `OWNED_PATHS`. גם אם המודל יכתוב לשם, זה לא ייכנס לקומיט ויישאר גלוי בעץ העבודה.
2. `EXTRA_PROMPT_WRITE_RULES` אומר לו את זה מפורשות, כשלב ממוספר.

`apply-to-vault.py` מעדכן את `records.md` ואת `4-learned/README.md` כדי שהחוק בקבצים יתאר את מה שקורה בפועל. כלל שנשאר על הדיסק וסותר את ההתנהגות הוא הוראה מתחרה שהמייקר הבא יסמוך עליה.

## חמישה דברים שעוברים כמו שהם, ואסור לפרק אותם

הם נראים כמו הגנת יתר עד היום שכל אחד מהם נופל.

**המודל שליח ולא מחליט.** המעלה מפוצל לארבעה שלבים: המודל מונה, באש סופרת ועושה הפרש קבוצות אמיתי, המודל מביא פגישה אחת בכל הפעלה, ופייתון מודד ומפצל. ספירה היא לא שיקול דעת, ומודל שסופר לא נכשל, הוא מדווח.

**שלב אימות נפרד שמוכיח שהמקור נקרא.** הסופג מריץ הרצה שנייה, זעירה, שכל תפקידה לרשום את תיקיית הדרייב לקובץ, ובאש סופרת את הקובץ. `seen` שנקרא מהשורה שהמודל כתב על עצמו הוא המודל בודק את עצמו.

**פעימת חיים ושומר.** ריצה שמפסיקה להתחיל לא יכולה להתלונן. קובץ פעימה בתוך הוולט, ושבוע שקט של פגישות נראה בדיוק כמו צינור מת בלעדיו.

**נקודת שחזור בלי לגרוף עבודה פתוחה, והרשאות מינימום.** אין קומיט מקדים. נקודת השחזור היא ה-HEAD הקיים, ומה שהריצה כתבה מחושב אחר כך מהפרש גיבובים, ורק הנתיבים האלה נכנסים לקומיט. נתיב שכבר היה מלוכלך לפני הריצה לעולם לא נכנס, כי `git add` מעלה קבצים שלמים ולא שורות.

**ארבעה דברים לא נכתבים לבד אלא הולכים לתור:** מספר שלא אומת, כל דבר ששם בו לקוח, כסף שלא מאושר ב-`1-me/`, וכל מה שנחתך כזיהום.

## כללי העבודה שלך במשימה הזו

לפני כל שלב, משפט אחד לבעלים על מה עומד לקרות ומה יידרש ממנו. הוא לא טכני.

עצור בכל מקום שהוא צריך ללחוץ: אישור מחבר בדפדפן, הפעלת מחבר ב-claude.ai, הרשאת התראות, טעינת ג'וב.

אל תניח ששום דבר עובד לפני שהוכחת אותו בקריאה חיה אחת.

## שלב 0, PREFLIGHT

**א. ודא שזו באמת תיקיית MAKERS.** חייבים להתקיים `CLAUDE.md`, `2-makers/morty/morty.md`, `4-learned/state.md` ו-`.claude/rules/records.md`. אם אחד חסר, אמור שזו לא תיקיית MAKERS ועצור בלי לגעת בכלום.

**ב. גיט.** הסופג מסרב לרוץ על תיקייה שאינה ריפו. אם אין, בקש אישור ל-`git init`, כי זה שינוי במכונה שלו. אם יש, אל תיגע.

**ג. הבינארי של קלוד.** ודא שהסקריפטים מוצאים אותו, והרץ `DRY_RUN=1` על שניהם בסוף שלב 2.

**ד. שמות הכלים האמיתיים.** הדפס את שמות כלי ה-MCP שזמינים בפועל, וקח מהם את הקידומות המדויקות. אל תניח אותן. `[לאמת]`

**ה. התיקייה בדרייב.** אם אין לו תיקייה ייעודית, פתח אחת ב-Drive וקח את המזהה מה-URL. אם יש, קח את הקיימת.

## שלב 1, חיבור המחברים

Wispr Flow ו-Google Drive, שניהם דרך claude.ai, ושניהם באישור שלו בדפדפן. הוכח כל אחד בקריאה חיה אחת לפני שאתה ממשיך.

## שלב 2, חיווט המערכת

הרץ מתוך הערכה:

```
python3 makers/apply-to-vault.py "/נתיב/לתיקיית/MAKERS"
```

זה מעתיק את המתעד, את שני הסקילים, את שלוש הדלתות, את שני קבצי המאגר, את **מדריך התשתית** ואת **הסקריפטים עצמם אל `.claude/scripts/` בתוך הוולט**, ואז מחיל את התיקונים הכירורגיים על קבצים שכבר קיימים אצלו. הוא אידמפוטנטי ולא דורס שום קובץ קיים.

⛔ **מכאן והלאה הסקריפטים רצים מהוולט, והריפו המשוכפל הוא משלוח חד פעמי שאפשר למחוק.** זה לא סידור: כל סקריפט פותר את התיקייה של עצמו, ולכן ההגדרות, קובץ המעקב ופעימת החיים נוחתים לידו. בתוך הוולט הם בתוך גיט, כלומר מגובים והפיכים; מחוץ לו הם לבד, וקובץ מעקב שאבד מעלה מחדש כל פגישה שהבעלים אי פעם הקליט. ועוד: הבעלים פותח את הוולט בכל יום ואף פעם לא את הריפו, ומכונה שהוא לא רואה היא מכונה שהוא לא יכול לכבות.

**שלוש טבלאות מפרטות את הצוות, לא אחת, ושתיים מהן נטענות לפני שהקובץ של מורטי בכלל נפתח.** הטבלה ב-`CLAUDE.md` נקראת בפתיחת כל סשן ושם נופלת ההחלטה הראשונה לאן ללכת, `system.md` מתארת את צורת הצוות, והטבלה של מורטי היא הניתוב עצמו. **בדיקת המערכת מאמתת רק את זו של מורטי**, אז מאקר שקיים רק בה מנותב אליו לפעמים. לפעמים גרוע יותר מאף פעם, כי זה נקרא כמערכת הפכפכה ולא כמערכת חסרה.

אם עוגן לא נמצא, הסקריפט **נעצר** במקום לנחש איפה השורה הולכת. זה אומר שהוולט שלו בגרסה שהמטען לא נכתב מולה, ואז קרא את הקובץ ותקן ביד.

## שלב 3, מילוי ה-config

ההגדרות יושבות ב-`.claude/scripts/config.sh` **בתוך הוולט של הבעלים**, לא בריפו המשוכפל.

העתק `.claude/scripts/config.example.sh` ל-`.claude/scripts/config.sh` ומלא. **הערכים שלמטה הם מה שהופך את הערכה לגרסת MAKERS, והם לא אופציונליים.**

```bash
VAULT="/נתיב/מלא/לתיקיית/MAKERS/שלו"
DRIVE_FOLDER_ID="<מזהה התיקייה מ-URL של Drive>"
LABEL_PREFIX="com.makers.transcripts"
TIMEZONE="Asia/Jerusalem"

PROTOCOL_FILE=".claude/skills/absorb-transcript/SKILL.md"
REPORTS_DIR="3-work/now/transcript-absorption"

OWNED_PATHS="
1-me/*.md
4-learned/log.md
2-makers/*/craft.md
3-work/now/transcript-absorption/*
5-library/messaging/*
5-library/objections/*
.claude/scripts/absorber-state.txt
.claude/scripts/absorber-heartbeat.txt
"

EXTRA_PROMPT_FACTS="- You are the Scribe. Read 2-makers/scribe/scribe.md as well; it carries the filled-in routing table and the reasoning behind every gate.
- 4-learned/state.md is NOT yours. You propose rules; the Archivist writes them."

EXTRA_PROMPT_WRITE_RULES="
STEP 6b. THE MAKERS ROUTING IS ALREADY FILLED IN. Use the table in 2-makers/scribe/scribe.md step 7 verbatim: real paths, not roles. Run the selling-and-explaining slot on EVERY transcript (it may be empty) and route it to 5-library/messaging/messaging.md, 5-library/objections/objections.md or 1-me/offers.md. Both craft gates bind: the admission test ('when X, do Y instead of Z', no Z means news not craft) and the supersede gate (search the file for the SUBJECT first; if it is there, rewrite the block or drop the line, never append beside it).
⛔ STEP 6c. You may write 4-learned/log.md. You may NOT write 4-learned/state.md, ever, for any reason. Every standing rule you would put there goes instead into the report under a section headed 'כללים מוצעים' AND as a new row appended at the bottom of 3-work/now/transcript-absorption/_process/proposed-rules.md, carrying today's date, the bullet in the owner's language, and this report's path. Create that file with a one-line header if it does not exist.
STEP 6d. The report goes at the top level of 3-work/now/transcript-absorption/ named YYYY-MM-DD-קליטת-תמלולים.md, and the source transcripts you absorbed are NOT moved (they live in Drive, not on this machine). Plain Hebrew markdown, no <div> wrapper of any kind, no long dashes."
```

**`PROTOCOL_FILE` מצביע על הסקיל שלו, לא על קובץ גנרי שמעתיקים לוולט.** פרוטוקול אחד, ואותו אחד רץ גם כשהוא מפעיל את המתעד ביד.

## שלב 4, זריעת קובץ המעקב, לפני הריצה החיה הראשונה

```
SEED_ONLY=1 bash .claude/scripts/transcript-absorber.sh
```

זה מונה את התיקייה ורושם כל מזהה ככבר מטופל, בלי לספוג כלום ובלי לכתוב לוולט. בלי זה, הריצה הראשונה סופגת שנה של פגישות בלילה אחד.

**ואותו דבר למעלה:** הרצה ראשונה עם `ALLOW_EMPTY_STATE=1` רק אחרי שהוא ראה ואישר, אחרת כל היסטוריית הפגישות שלו נשפכת לתיקייה שאולי מישהו נוסף קורא.

## שלב 5, הרשאת התראות של macOS

הכשל הרועש הוא התראה. בקש ממנו לאשר אותה, והוכח שהיא מופיעה. `[לאמת]`

## שלב 6, ריצה חיה אחת מקצה לקצה, מולו

מחק **מזהה אחד** מ-`.claude/scripts/absorber-state.txt` ביד, והרץ:

```
bash .claude/scripts/transcript-absorber.sh
```

הכותרת של קובץ המעקב אומרת את זה בעצמה: מוחקים מזהה כדי לאלץ ספיגה מחדש.

⛔ **ארבעה דברים נבדקים, ורק אז זה גמור:**

1. דוח מתוארך נחת ב-`3-work/now/transcript-absorption/`
2. הדוח מחזיק מספרים פתוחים ותיוגי ודאות. **אם אין לו אף אחד מהשניים, השערים לא רצו**
3. `4-learned/state.md` **לא השתנה**, וההצעות יושבות ב-`_process/proposed-rules.md`
4. `git log -1 --stat` מראה קומיט שמחזיק רק את הנתיבים של הריצה

## שלב 7, תזמון

טען את שני הג'ובים מ-`launchd/` שבערכה, כשהם מצביעים על הנתיבים **בתוך הוולט**. המעלה תמיד לפני הסופג, ובמרווח הגדול ביותר שאפשר. **ריצת הבוקר של הסופג היא הראשית, וריצת הערב היא מאמץ נוסף בלבד**, כי בניית המסמכים אצל המעלה לוקחת זמן ומרווח קצר לא מספיק לה בפועל. אל תבטיח לו ספיגה באותו ערב, ואמור לו בקול את שתי השעות שבחרת.

## שלב 8, הרץ אצלו `/check-the-system`

לא אתה מדווח שזה תקין, הבדיקה שלו מדווחת. המתעד חייב להופיע בטבלת הצוות של מורטי, ומספר הפקודות חייב להיות שווה למספר הסקילים ועוד מספר המייקרים.

## שלב 9, סגירה מולו

ארבע שורות קצרות, בשפה שלו:

הפגישות שלו מגיעות לצוות פעמיים ביום, לבד. הצוות מציע כללים ולא כותב אותם לרשימה הקבועה. כל מה שריצה עשתה מתבטל בשורה אחת, ותן לו אותה. כיבוי הוא שני `launchctl bootout` ומחיקת שני קבצים, ושום דבר כאן לא בלתי הפיך.

הוסף שורה ל-`tools.md` לפי `/connect-a-tool`: מה זה, מה זה יכול, שזה **כותב**, התאריך, ואיך מכבים.

## רשימת ה-`[לאמת]`

1. הקידומות המדויקות של שמות כלי ה-MCP אצלו.
2. שהמחברים צפים תחת `-p` בחשבון שלו.
3. ש-MCP בסקופ USER נראה לריצת launchd מתיקיית MAKERS.
4. שהתראת `osascript` מופיעה תחת launchd אחרי מתן הרשאה.
5. שהשורות שהסופג כתב נחתו בקבצי המייקרים שבטבלה, ולא בקובץ שהומצא בריצה.
6. חידוש הטוקן של מחבר Drive לאורך זמן תחת ריצה בלי מסך.

## שלושה דברים שאסור לך לעשות

אל תיתן למודל לכתוב לקובץ המעקב או לגעת בגיט. ההפרדה הזאת היא מה שמונע פגישה שנספגת פעמיים.

אל תריץ את הסופג על תיקייה שאינה ריפו גיט. הוא מסרב, ובצדק.

ואל תוסיף את `4-learned/state.md` ל-`OWNED_PATHS`, בשום מצב, גם אם נראה שחסר שם משהו. התקרה היא הסיבה שהקובץ הזה עדיין עובד אצלו.
