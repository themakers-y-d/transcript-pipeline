<div dir="rtl" style="direction:rtl;text-align:right;unicode-bidi:plaintext">

# הוראות התקנה, לקלוד קוד

הקובץ הזה מיועד אליך, הסוכן שמתקין. הבעלים לא צריך לקרוא אותו. הוא מגדיר את הסדר המלא, מהרגע ששוכפלה התיקייה ועד צינור לילי חי, בלי לבזבז את הזמן של הבעלים ובלי להפיל שום דבר.

## עצור אם אתה לא קלוד קוד

הצינור הזה מריץ את הבינארי של קלוד קוד בכל לילה (`claude -p`), ומתחבר ל-Wispr Flow ול-Google Drive דרך מחברי ה-MCP של claude.ai. אם אתה קודקס, או כל סוכן אחר שאין לו את המחברים האלה ואת הבינארי הזה, אתה לא יכול להשלים את ההתקנה ולא יכול להריץ את הצינור.

במקרה כזה אל תתקין חצי מערכת. אמור לבעלים במשפט אחד שהערכה הזאת דורשת קלוד קוד, ושהוא צריך לפתוח אותה שם, ועצור.

## מה אתה מתקין

צינור תמלולים שרץ לבד כל לילה, בשני חלקים:

חלק ראשון, **המעלה**: מושך את פגישות Wispr Flow החדשות ומעלה כל אחת כמסמך Google Doc נפרד לתיקייה ב-Google Drive. חלק שני, **הסופג**: סורק את אותה תיקייה, ועל כל תמלול חדש מריץ פרוטוקול שמזהה מי היה בחדר, מפריד החלטות מדיבור, מסנן זיהום, בודק שכל מספר הגיוני, וכותב את מה שנלמד לתוך קבצי הזיכרון של הבעלים. בין השניים עומדת נקודת שחזור בגיט, כך שכל ספיגה שגויה נמצאת פקודה אחת מלהתבטל.

הכל רץ על התשתית של הבעלים בלבד: קלוד קוד על המחשב שלו, החשבון שלו ב-Wispr, ה-Drive שלו. אין שרת שלנו באמצע ואין שירות שלישי שמחזיק את המידע. התמלולים כן עוברים דרך Wispr ויושבים ב-Google Drive של הבעלים, וזה שווה לומר לו במפורש. אין טוקנים ואין סיסמאות בשום קובץ בערכה הזאת: ההרשאה עוברת דרך המחברים שהבעלים מאשר פעם אחת בדפדפן.

## הנחת היסוד שהשתנתה: אולי אין לו כלום

אל תניח שלבעלים כבר יש מערכת סוכנים, תיקיית זיכרון או קבצי הנחיות. הרבה מהאנשים שמתקינים את זה מתחילים מתיקייה ריקה, ובשבילם שלב 3 הוא מה שהופך את הערכה ממנוע בלי מכונית למערכת שלמה.

אם אין לו כלום, אתה מקים לו בית זיכרון מינימלי וזורע אותו. אם יש לו מערכת משלו, אתה מתחבר אליה ולא דורס אותה. את ההכרעה הזאת אתה עושה בשלב 0ה, לפני שאתה בונה משהו.

## כללי העבודה שלך במשימה הזו

זו עבודה על מערכת שרצה לבד וכותבת לקבצים האמיתיים של הבעלים. חמישה כללים קשיחים.

לפני כל שלב, הסבר לבעלים במשפט או שניים מה עומד לקרות ומה יידרש ממנו, בשפה רגועה ופשוטה. הוא לא טכני. הרגע אותו: התקנה מודרכת היא נורמלית וקלה.

עצור וחכה בכל מקום שהבעלים צריך ללחוץ או לאשר ידנית: אישור מחבר בדפדפן, הפעלת מחבר ב-claude.ai, מתן הרשאת התראות ל-macOS, טעינת ג'וב מתוזמן. אל תתקדם עד שהוא אומר שראה ואישר.

אל תניח ששום דבר עובד לפני שהוכחת אותו בקריאה חיה אחת. חיבור שנראה מחובר בטרמינל יכול להיות בלתי נגיש לריצה מתוזמנת בלי מסך. תוכיח, אל תניח.

בכל מקום המסומן למטה `[לאמת]`, אתה בודק ומדווח לבעלים מה קרה בפועל, לא מבטיח שזה עובד. אלה נקודות שלא הצלחנו להוכיח מראש שהן שורדות ריצה בלי מסך אצל כל אדם.

דרך החזרה פשוטה, ושווה לומר אותה כבר בהתחלה: מבטלים את טעינת שני הג'ובים, ומוחקים את קבצי ה-plist. תיקיית הזיכרון עצמה חוזרת בפקודת גיט אחת אחורה לנקודת שחזור. שום דבר לא בלתי הפיך.

## שלב 0, PREFLIGHT: בדיקה והוכחה, לפני שאתה בונה משהו

זה השלב שהופך את ההתקנה מ"בערך עובד" ל"עובד מהניסיון הראשון". אל תדלג עליו. ספר לבעלים שאתה עכשיו רק בודק, ולא משנה כלום.

**א. גיט על תיקיית הזיכרון.** התיקייה שהבעלים פתח בה את קלוד קוד היא בית הזיכרון שלו, וזו התיקייה שהסופג יכתוב לתוכה.

**לפני שאתה נוגע בגיט, שאל אותו במפורש:** "התיקייה שאנחנו בה עכשיו היא זו שתרצה שתהיה הזיכרון הקבוע שלך?" והצג לו את הנתיב המלא. אל תסתפק בכך שהיא אינה תיקיית הורדות. הרבה אנשים מדביקים את הפרומפט מאיפה שהם במקרה, וזו השאלה שמונעת התקנה שלמה בתיקייה אקראית. חכה לתשובה.

ואז ודא שהיא ריפו גיט. אם לא, הסבר לו במשפט אחד שאתה מדליק את רשת הביטחון שלו, והרץ `git init` שם. הסופג מסרב לרוץ בלי גיט, כי בלי נקודת שחזור אין דרך בטוחה לכתוב לקבצים שלו לבד.

אם התיקייה שנפתחה בבירור אינה מתאימה (תיקיית הורדות, שולחן העבודה, תיקיית מערכת), אל תריץ שם `git init`. עצור, הסבר בשורה אחת, ובקש ממנו לפתוח תיקייה ייעודית או לאשר לך ליצור אחת.

**ואזהרה אחת שקל לפספס:** אם התיקייה יושבת בתוך iCloud Drive, Dropbox או Google Drive מסונכרן, אמור לזה בקול. ריפו גיט בתוך תיקיית סנכרון מייצר קונפליקטים ושחיתות אינדקס, והמערכת הזאת כותבת לשם כל בוקר. המלץ על מיקום מקומי, למשל תחת `~/`, ותן לו להכריע.

**ב. איפה יושב הבינארי של claude.** בדוק את נתיב תוסף ה-VS Code‏ `~/.vscode/extensions/anthropic.claude-code-*/resources/native-binary/claude` (בחר את החדש ביותר עם `sort -V`), ואם אין, נסה `~/.local/bin/claude`, `/opt/homebrew/bin/claude`, `/usr/local/bin/claude`. הדפס את הנתיב שמצאת. הסבר לבעלים שהנתיב הזה משתנה בכל עדכון, ולכן הסקריפטים פותרים אותו מחדש בכל ריצה במקום לקבע אותו. ודא גם ש-`node --version` מחזיר 18 ומעלה ושגיט מותקן.

**ג. שמות הכלים האמיתיים.** הדפס את רשימת שמות כלי ה-MCP הזמינים לך עכשיו. מצא את הקידומת המדויקת של Wispr Flow ושל Google Drive. אל תנחש. `[לאמת]` הקידומת של Drive תלויה בדרך שבה המחבר נוסף: המחבר של claude.ai נותן `mcp__claude_ai_Google_Drive__`, אבל Drive שנוסף דרך MCP אחר ייתן קידומת שונה. את הקידומות שמצאת תכתוב בהמשך ל-config.sh; משם הסקריפטים בונים את רשימת הכלים המותרים.

**ד. הוכח כל מחבר בקריאה חיה אחת.** קרא ל-search של Wispr (limit 3) והדפס כמה פגישות חזרו. קרא ל-search_files של Drive על תיקיית היעד והדפס שם של קובץ אחד שחזר. אם אחד מהם מחזיר ריק או לא זמין, **עצור.** הסבר לבעלים בפשטות מה עליו לעשות ביד: להפעיל את מחבר Google Drive ב-claude.ai (Settings ‑ Connectors ‑ Google Drive) ולהישאר מחובר לאותו חשבון, או להשלים את התחברות ה-MCP של Wispr בדפדפן. חכה שיאשר לפני שתמשיך. זה נורמלי: מחברים דורשים אישור אנושי חד-פעמי.

**ה. מפה את מערכת הזיכרון של הבעלים.** זו ההכרעה שקובעת את שלב 3, ולכן היא חלק מה-preflight ולא ניחוש בדיעבד.

סרוק את תיקיית הזיכרון וחפש סימנים למערכת קיימת: `CLAUDE.md`, תיקיית `.claude/`, תיקיות של סוכנים או קבצי הנחיות, קבצי זיכרון או יומן, מבנה תיקיות שנראה מכוון. אל תסיק ממספר הקבצים לבד.

ואז שאל את הבעלים שאלה אחת פשוטה: "יש לך כבר קבצים שקלוד קורא כדי לזכור איך אתה עובד, או שאנחנו מתחילים מאפס?" השאלה הזאת גוברת על הסריקה, כי היא מה שהוא יודע ואתה לא.

שתי התוצאות: **יש לו מערכת** ואז בשלב 3 אתה ממפה אליה ולא בונה חדשה, או **אין לו כלום** ואז בשלב 3 אתה מקים וזורע. אמור לו איזו משתי הדרכים אתם הולכים, במשפט.

**ו. איפה תשב הערכה.** שכפל את הערכה לתיקייה נפרדת **מחוץ** לתיקיית הזיכרון, למשל `~/transcript-pipeline`. אל תשאיר אותה בתוך תיקיית הזיכרון: הסופג מבצע `git add -A` על תיקיית הזיכרון בכל לילה, וערכה שיושבת בפנים נגררת לתוך הקומיטים שלו ומייצרת ריפו בתוך ריפו. הדפס לבעלים את הנתיב המלא של הערכה בשורה אחת, כדי שיידע איפה היא.

**סיכום ל-preflight:** אמור לבעלים בארבע שורות מה מצאת: נתיב הבינארי, שתי קידומות המחברים, ששני המחברים החזירו מידע אמיתי, ואם יש לו מערכת קיימת או שאתם מתחילים מאפס. רק אז אמור שאתה מוכן לבנות.

`[לאמת]` שרשימת שמות הכלים והקריאה החיה זמינות באותו session שממנו ייבנו הסקריפטים, ושהמחבר יצוף מאוחר יותר תחת ריצת `claude -p` בלי מסך בדיוק כמו שהוא צף כאן. אצל מי שבנה את הערכה זה רץ יציב, אבל תבדוק ותדווח, אל תבטיח.

## שלב 1, חיבור המחברים

אם ה-preflight כבר הוכיח את שני המחברים, דלג. אם לא, לווה את הבעלים אחד-אחד, ותעצור בכל לחיצה.

**Wispr:** הוסף את מחבר Wispr Flow ב-MCP בכתובת `https://api.wisprflow.ai/connect/mcp` ב-scope של USER, כדי שריצה מתוזמנת מכל תיקייה תראה אותו. אמור לבעלים ברור מתי ייפתח חלון התחברות בדפדפן ושעליו לאשר שם ביד, ואז חכה. אחרי שיאשר, הוכח בהצגת הפגישה האחרונה שתומללה. `[לאמת]` שהוספת ה-MCP ב-USER scope באמת נראית לריצת launchd שרצה מתוך תיקיית הזיכרון; אם לא, ודא ש-`.mcp.json` יושב בתיקייה שהסקריפט עושה לה cd.

**Drive:** אמור לבעלים בפשטות שמחבר Google Drive מופעל בתוך claude.ai (Settings ‑ Connectors ‑ Google Drive), לא מהטרמינל, ושעליו להדליק אותו שם ולהישאר מחובר לאותו חשבון. חכה שיאשר. הוכח בהצגת שם של קובץ אחד שאתה רואה בתיקייה.

הרגע אותו בכל צעד שאישור ידני חד-פעמי הוא נורמלי, ושברגע שנעשה, שום דבר כאן לא דורש עוד לחיצות ממנו.

## שלב 2, מילוי ה-config

העתק את `scripts/config.example.sh` ל-`scripts/config.sh` ומלא אותו לבעלים הזה. עבור ערך-ערך:

- `VAULT`: הנתיב המוחלט לתיקיית הזיכרון, זו שהבעלים פתח בה את קלוד קוד.
- `DRIVE_FOLDER_ID`: מזהה התיקייה ב-Drive שאליה יעלו התמלולים. שאל את הבעלים לאיזו תיקייה להעלות; אם אין עדיין, בקש שייצור תיקייה ב-Drive. המזהה הוא החלק האחרון בכתובת התיקייה.
- `WISPR_TOOL_PREFIX`, `DRIVE_TOOL_PREFIX`: הקידומות המדויקות שהדפסת ב-preflight. אלה קריטיות; קידומת שגויה חוסמת את הכלי בשקט.
- `LABEL_PREFIX`: שם לזיהוי שני הג'ובים, למשל `com.<שם-הבעלים>.transcript`. אל תשתמש ב-`com.example`.
- `TIMEZONE`: אזור הזמן של הבעלים כשם IANA אמיתי, למשל `Asia/Jerusalem`. לא היסט קבוע: היסט קבוע שגוי חצי שנה.
- שאר הערכים: השאר בברירת המחדל אלא אם לבעלים יש סיבה לשנות.

הסבר לבעלים בשורה מה עשית: הקובץ הזה הוא רק הגדרות, בלי סודות.

## שלב 3, בית הזיכרון: להקים או להתחבר

זה השלב שהופך את הערכה למערכת. בלעדיו הסופג יגיע בלילה עם מסקנות אמיתיות ולא יהיה לו לאן לכתוב אותן, ואז הוא ימציא שמות קבצים חדשים בכל לילה ואתה תקבל ערימה במקום זיכרון.

לפי מה שהכרעת ב-0ה, לך לאחד משני המסלולים.

### מסלול א, לבעלים שאין לו כלום

צור את המבנה הזה בתיקיית הזיכרון. שישה קבצים ושתי תיקיות, זה הכל:

```
memory/profile.md        מי הבעלים, מה העסק, עובדות קבועות
memory/strategy.md       מיצוב, הצעה, קהל, התנגדויות
memory/writing.md        מסרים, ניסוחים, מה עבד ומה לא
memory/design.md         מראה, מותג, פריסה
memory/automation.md     כלים, מערכות, תקלות ותיקונים
memory/rules.md          כללים קבועים להתנהגות המערכת
transcripts/reports/     דוח מתוארך אחד לכל ריצה של הסופג
```

כל קובץ נפתח בכותרת אחת שאומרת מה הוא מחזיק, ומתחתיה כותרת `## יומן` ריקה. הסופג מוסיף שורות מתוארכות מתחתיה ולעולם לא כותב מחדש את מה שכבר שם.

**ואז זרע את הפרופיל, ואל תדלג על זה.** קובץ פרופיל ריק הוא לא רק חסר, הוא שובר שער בטיחות: שער המספרים בפרוטוקול מסתמך על כך שיש במה להצליב מספר שנשמע בפגישה, ובלי פרופיל כל מספר יסומן לאימות לנצח.

שאל את הבעלים ארבע שאלות קצרות, אחת-אחת, בשיחה ולא כטופס. במה הוא עוסק ולמי. איך קוראים לעסק או למותג, ואיזה שמות חוזרים אצלו (שותפים, מוצרים, לקוחות קבועים). מה הוא מוכר ובאיזה טווח מחירים בערך. מה הוא רוצה שהמערכת תזכור עליו שאף אחד לא יודע מבחוץ.

כתוב את התשובות ל-`memory/profile.md` בשפה שלו, לא בשפה שלך. אמור לו שזה ייקח דקה ושזה מה שגורם ללילה הראשון להיות שווה משהו.

### מסלול ב, לבעלים שיש לו מערכת

אל תקים כלום ואל תדרוס כלום. מפה את הקבצים הקיימים שלו לששת התפקידים שלמעלה: מי מחזיק אצלו כתיבה, מי מחזיק אסטרטגיה, מי מחזיק עיצוב, מי מחזיק אוטומציה, מי מחזיק את הפרופיל, ומי מחזיק את הכללים.

תפקיד שאין לו בית אצלו הוא ממצא, לא תקלה. אמור לו בשורה אחת שאין קובץ שמחזיק את זה, ושאל אם להקים אחד או לנתב לפרופיל.

ודא רק שיש יעד לדוחות. אם אין, צור `transcripts/reports/`.

### בשני המסלולים

**סגור את הגיט לפני שאתה ממשיך.** אם התיקייה קיבלה `git init` רק עכשיו, אין בה עדיין אף קומיט, ולכן אין נקודת שחזור לחזור אליה. הרץ `git add -A` ו-`git commit` על מה שיש, גם אם זה רק השלד שהקמת. זו הנקודה היחידה בהתקנה שבה הלילה הראשון יכול לרוץ בלי רשת ביטחון, ושורה אחת סוגרת אותה.

הצג לבעלים את רשימת היעדים הסופית לפני שאתה ממשיך, כדי שיראה לאן המערכת עומדת לכתוב בלילה. זו נקודת העצירה היחידה בשלב הזה.

## שלב 4, זריעת קובץ המעקב, לפני הריצה החיה הראשונה

זה שלב קריטי, ולכן עצור והסבר אותו. אם קובץ המעקב של המעלה ריק בריצה הראשונה, כל פגישה שכבר קיימת בתיקייה תיראה חדשה ותועלה שוב, ותיווצר כפילות בתיקייה שאולי מישהו אחר קורא.

לכן, לפני הריצה החיה הראשונה: קרא את תיקיית ה-Drive וגם את פגישות Wispr הקיימות, וכתוב את מזהי הפגישות שכבר יש להן מסמך בתיקייה לתוך `scripts/uploader-state.txt`, אחד בשורה. אם הבעלים מתחיל מאפס ואין עדיין שום פגישה בתיקייה, זו באמת התקנה נקייה, ואפשר להריץ פעם אחת עם `ALLOW_EMPTY_STATE=1` (ורק פעם אחת). הסבר לבעלים למה הזריעה קיימת: היא מונעת גל של כפילויות בריצה הראשונה.

## שלב 5, הרשאת התראות של macOS

אמור לבעלים בפשטות שצריך לאשר ל-macOS להציג התראות, כדי שכשל בלילה יצעק במקום להיבלע. שגר עכשיו התראת בדיקה אחת עם `osascript` כדי שהוא יראה אותה ויאשר ב-System Settings ‑ Notifications. חכה שיאשר שראה. הבהר: גם אם התראה נבלעת פעם, בדיקת פתיחת הסשן בבוקר עדיין תגיד שהצינור מת, כי סימן החיים לא תלוי בהרשאה. `[לאמת]` שהתראת `osascript` אכן מופיעה תחת launchd אחרי מתן ההרשאה, ולא נבלעת בשקט.

## שלב 6, פרוטוקול הספיגה

העתק את `transcript-protocol.md` אל תוך תיקיית הזיכרון, במקום ש-`PROTOCOL_FILE` ב-config מצביע אליו (ברירת המחדל: `.claude/transcript-protocol.md`).

ואז **מלא את טבלת הניתוב שבשלב 7 של הפרוטוקול בנתיבים האמיתיים** שקבעת בשלב 3. אל תשאיר את ברירות המחדל הגנריות: "your writing memory" אינו נתיב, והסופג שרץ בלילה בלי אדם שמסתכל צריך לדעת בדיוק לאיזה קובץ לכתוב. במסלול א אלה קבצי `memory/`; במסלול ב אלה הקבצים שלו.

אל תשנה את שערי הבטיחות: הזיהום, אורך המינימום, כלל המספר-הלא-מאומת, וארבעת הדברים שלא נכתבים בלי השגחה. הסבר לבעלים בקצרה שהקובץ הזה הוא ה"מצפון" של הסופג.

## שלב 7, חיווט הסקריפטים ובדיקת תחביר

ודא ששני הסקריפטים ניתנים להרצה (`chmod +x`). הרץ בדיקת תחביר על שניהם: `bash -n scripts/transcript-uploader.sh` ו-`bash -n scripts/transcript-absorber.sh`. אם `shellcheck` מותקן, הרץ גם אותו ותקן אזהרות אמיתיות. הרץ כל סקריפט פעם אחת ב-`DRY_RUN=1` כדי לראות שהוא מוצא את הבינארי, קורא את ה-config, ומדפיס את הנתיבים הנכונים, בלי לגעת בשום דבר חי.

## שלב 8, ריצה חיה אחת מקצה לקצה, מול הבעלים

זו ההוכחה. dry run הוא לא הוכחה.

הרץ את **המעלה** פעם אחת בפועל (בלי `DRY_RUN`). הראה לבעלים את מסמכי ה-Google Doc שנוצרו בתיקייה, ואת קובץ המעקב שנכתב. `[לאמת]` **הבאג הפתוח האמיתי:** אם לבעלים יש פגישה ארוכה מאוד (עשרות אלפי תווים), זו הבדיקה שמוכיחה שהחלוקה לחלקים עובדת. ודא שהיא עלתה כ-N מסמכים עם הכותרת ` (חלק K מתוך N)`, ולא נכשלה בשקט. אם היא כן נכשלה, כייל את `CHUNK_CHAR_LIMIT` כלפי מטה ונסה שוב. דווח לבעלים מה קרה בפועל.

ואז הרץ את **הסופג** פעם אחת על תמלול אמיתי אחד. הראה לבעלים: את commit נקודת השחזור, את הקבצים שהשתנו, ואת הדוח המתוארך שנכתב. הראה לו שאפשר לבטל הכל בפקודת גיט אחת (`git revert`). `[לאמת]` אם מסמך ארוך חזר חתוך מ-read_file_content, שה-pagination אכן משיג את הגוף המלא לפני הסיווג, ושמערכת רב-חלקית אוחדה לפגישה אחת ולא סווגה כשלושה חדרים.

**ולבעלים שהתחיל מאפס, בדוק גם את זה:** שהשורות באמת נחתו בקבצי `memory/` שהקמת ולא בקובץ חדש שהומצא תוך כדי. אם הומצא קובץ, הטבלה בשלב 6 לא מולאה כמו שצריך. חזור ותקן אותה לפני התזמון.

## שלב 9, תזמון בענן המקומי (launchd)

מלא את שני קבצי ה-plist בתיקיית `launchd/` מתוך התבניות: `__LABEL__`, `__SCRIPT_PATH__`, `__PATH__`, `__LOG_DIR__`. ה-PATH חייב לכלול את `~/.local/bin` ואת `/opt/homebrew/bin` עם הנתיבים האמיתיים של הבעלים, אחרת launchd לא ימצא את `node` ואת `git`. העתק את שני הקבצים ל-`~/Library/LaunchAgents/` בשמות שתואמים ל-`LABEL_PREFIX`, וטען אותם. הבעלים אולי יצטרך להריץ את פקודת ה-`launchctl bootstrap` בעצמו בטרמינל אם שכבת ההרשאות חוסמת אותך; אם כן, תן לו את הפקודה המדויקת והסבר, אל תעקוף.

`[לאמת]` תזמון: המעלה ב-22:10 לא תמיד מסיים לפני שהסופג של אותו ערב סורק. ההנחה ש-25 דקות מספיקות שגויה בפועל אצל מי שבנה את זה. לכן ריצת הבוקר של הסופג היא הראשית, וריצת הערב היא מאמץ נוסף בלבד. בדוק את זמני הסיום האמיתיים אצל הבעלים והתאם את השעות; אל תבטיח ספיגה באותו ערב.

## שלב 10, בדיקת פתיחת הסשן, לסגור את החור השקט

ג'וב לילי שמפסיק להתחיל לא יכול להתלונן. רק גיל תופס את זה.

הוסף לקובץ `CLAUDE.md` שבתיקיית הזיכרון הוראה שבכל פתיחת סשן קלוד יקרא את **שני** סימני החיים (`scripts/uploader-heartbeat.txt` ו-`scripts/absorber-heartbeat.txt`, בנתיבים המלאים שלהם בערכה), ואם הריצה האחרונה של אחד מהם בת יותר מיומיים או מסומנת כשל, יאמר זאת בשורה אחת פשוטה לפני כל דבר אחר. שני סימני החיים, לא אחד: אצל מי שבנה את זה נבדק רק הסופג, וזה חור שכדאי לסגור.

אם אין `CLAUDE.md` בתיקייה, צור אותו. במסלול א זה גם המקום להוסיף שתי שורות שמפנות את קלוד לקבצי `memory/` ולתיקיית הדוחות, כדי שסשן רגיל ידע שהם קיימים.

## שלב 11, סגירה מול הבעלים

לפני שאתה מכריז שסיימת, אמור לבעלים בכמה שורות מה חי עכשיו ומה חשוב לדעת:

- כל לילה הפגישות החדשות עולות לתיקייה, ובבוקר נספגות לקבצי הזיכרון שלו. הוא לא צריך לעשות כלום.
- איפה הקבצים האלה יושבים, בשם, כדי שיוכל לפתוח אותם ולראות מה נכתב.
- הצינור לעולם לא מוחק, לעולם לא מעלה פעמיים, ולעולם לא כותב מספר שלא הצליח לאמת.
- לפני כל ספיגה יש נקודת שחזור. ביטול הוא `git revert` אחד, והראה לו את הפקודה.
- אם משהו נשבר, זה יצוף: התראה בערב, וסימן החיים בפתיחת הסשן בבוקר.
- תיקיית הערכה מחזיקה את קובצי המעקב שזוכרים מה כבר עלה ומה כבר נספג. אל תמחק אותה: בלעדיהם הריצה הבאה תחשוב שהכל חדש ותעלה הכל שוב.
- כיבוי מלא: `launchctl bootout` לשני הג'ובים. שום דבר לא מתפרק.
- ולבסוף, בשורה אחת: הכפתור ב-Wispr נשאר שלו. שום דבר כאן לא מקליט בשבילו.

## רשימת ה-`[לאמת]`, שאתה בודק ומדווח ולא מניח

1. חידוש הטוקן של מחבר Drive לאורך זמן תחת ריצה בלי מסך.
2. הקידומת המדויקת של שמות כלי ה-Drive אצל הבעלים (מגלים ב-preflight, לא מניחים).
3. שהמחבר צף תחת `-p` באופן זהה בחשבון הזה.
4. ש-MCP ב-USER scope נראה לריצת launchd מתיקיית הזיכרון.
5. הסף המדויק של `create_file`, והוכחה שהחלוקה לחלקים פותרת פגישה ארוכה מאוד בפועל.
6. זמני הסיום האמיתיים של המעלה, לכיול שעת הסופג.
7. ש-pagination משיג את הגוף המלא מ-`read_file_content`.
8. שהתראת `osascript` מופיעה תחת launchd אחרי מתן הרשאה.
9. שהשורות שהסופג כתב נחתו בקבצי הזיכרון שקבעת, ולא בקובץ שהומצא בריצה.

## שני דברים שאסור לך לעשות

אל תיתן למודל לכתוב לקובץ המעקב או לגעת בגיט. ההפרדה הזאת, שהסקריפט רושם ולא המודל, היא מה שמונע פגישה שנספגת פעמיים או עולה פעמיים. היא לא קישוט.

אל תריץ את הסופג על תיקייה שאינה ריפו גיט. הסקריפט מסרב, ובצדק: בלי נקודת שחזור אין דרך בטוחה לכתוב לקבצים של הבעלים לבד.

</div>
